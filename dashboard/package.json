{
  "name": "fox-shield-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "fox-shield v1.0 dashboard — live stats, ban list, aggressive mode switch (GitHub Pages)",
  "scripts": {
    "dev": "bun node_modules/vite/bin/vite.js",
    "build": "bun node_modules/typescript/bin/tsc --noEmit && bun node_modules/vite/bin/vite.js build",
    "preview": "bun node_modules/vite/bin/vite.js preview",
    "deploy": "bun node_modules/vite/bin/vite.js build && bun node_modules/gh-pages/bin/gh-pages.js -d dist"
  },
  "dependencies": {
    "preact": "^10.22.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.0",
    "@types/node": "^20.14.0",
    "gh-pages": "^6.1.1",
    "typescript": "^5.4.0",
    "vite": "^5.3.0"
  }
}
